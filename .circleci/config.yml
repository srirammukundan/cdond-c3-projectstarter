version: 2.1

jobs:
  build-frontend:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - restore_cache:
          keys: [frontend-build-SM]
      - run:
          name: Build front-end
          command: |
            npm install
            npm run build
      - save_cache:
          paths: [frontend/node_modules]
          key: frontend-build-SM

  build-backend:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - restore_cache:
          keys: [backend-build-SM]
      - run:
          name: Back-end build
          command: |
             npm install
             npm run build
      - save_cache:
          paths: [backend/node_modules]
          key: backend-build-SM

workflows:
  default:
    jobs:
      - build-frontend
      - build-backend